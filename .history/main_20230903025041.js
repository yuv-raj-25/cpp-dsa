function solve(arr, idx, res){
    if(idx == arr.length)
        console.log(res);
    
    for(int i = idx; i < arr.length; )    
}
function perm(arr){
    let res = [];
    solve(arr, 0, res)

}

const arr = [1,2,3];

